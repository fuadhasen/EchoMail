import { useState } from "react";

import type { ActivityItems } from "@/type";
import { Bell, Eye, Loader2, Mail, Reply } from "lucide-react";

interface RecentActivityProps {
  initialItems: ActivityItems[];
}

// const efficiencyStrokeDashoffset =
// circumference - (followUpEfficiency / 100) * circumference;
// 1 widget
const RecentActivity = () => {
  return <div>Efficiency Score will be returned here</div>;
};

export default RecentActivity;
